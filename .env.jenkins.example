# Jenkins Environment Configuration
# Copy this file to .env.jenkins and update with your values

# Jenkins Configuration
JENKINS_URL=http://localhost:8080
JENKINS_AGENT_SECRET=change-this-secret

# Docker Registry
DOCKER_REGISTRY=localhost:5001
DOCKER_REGISTRY_USERNAME=admin
DOCKER_REGISTRY_PASSWORD=admin

# Image Configuration
IMAGE_PREFIX=club-management

# AWS Configuration (if deploying to AWS)
AWS_REGION=us-east-1
AWS_ACCOUNT_ID=123456789012

# Deployment Hosts
STAGING_HOST=staging.example.com
PROD_HOST=prod.example.com

# Notification (optional)
SLACK_WEBHOOK_URL=
EMAIL_RECIPIENTS=team@example.com

# Deployment Method
# Options: docker-compose, k8s, ecs
DEPLOYMENT_METHOD=docker-compose
