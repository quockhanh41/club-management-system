{
  "name": "club-management-system",
  "version": "1.0.0",
  "description": "Club Management System - Microservices Architecture",
  "scripts": {
    "dev": "docker-compose up",
    "build": "docker-compose build",
    "test": "npm run test:unit && npm run test:e2e",
    "test:unit": "npm run test:services",
    "test:services": "cd services/auth && npm test && cd ../club && npm test && cd ../event && npm test",
    "test:e2e": "playwright test",
    "test:e2e:ui": "playwright test --ui",
    "test:e2e:headed": "playwright test --headed",
    "install:e2e": "npx playwright install",
    "install:all": "npm run install:services && npm run install:e2e",
    "install:services": "cd services/auth && npm install && cd ../club && npm install && cd ../event && npm install && cd ../notify && npm install && cd ../../frontend && npm install"
  },
  "devDependencies": {
    "@playwright/test": "^1.57.0",
    "@types/node": "^20.19.30",
    "dotenv": "^17.2.1"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
